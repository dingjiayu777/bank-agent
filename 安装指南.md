# Python 和 Streamlit 环境安装指南

## 📋 环境检查

首先运行环境检查脚本：
```bash
check_environment.bat
```

## 🐍 步骤 1: 安装 Python

### Windows 系统

1. **下载 Python**
   - 访问官网：https://www.python.org/downloads/
   - 下载 Python 3.8 或更高版本（推荐 3.10 或 3.11）

2. **安装 Python**
   - 运行下载的安装程序
   - ⚠️ **重要**：勾选 "Add Python to PATH"（添加到环境变量）
   - 选择 "Install Now" 或自定义安装路径

3. **验证安装**
   ```bash
   python --version
   ```
   应该显示类似：`Python 3.11.x`

4. **如果 Python 未添加到 PATH**
   - 手动添加 Python 到系统环境变量
   - 路径通常是：`C:\Users\你的用户名\AppData\Local\Programs\Python\Python3xx\`
   - 或：`C:\Python3xx\`

## 📦 步骤 2: 安装项目依赖

### 方法 1: 使用 requirements.txt（推荐）

在项目目录下运行：
```bash
python -m pip install -r requirements.txt
```

### 方法 2: 单独安装 Streamlit

如果只需要 Streamlit：
```bash
python -m pip install streamlit
```

### 方法 3: 手动安装所有依赖

```bash
python -m pip install streamlit>=1.28.0
python -m pip install langchain>=0.1.0
python -m pip install langchain-openai>=0.0.5
python -m pip install openai>=1.0.0
python -m pip install python-dotenv>=1.0.0
```

## ✅ 验证安装

### 检查 Python
```bash
python --version
```

### 检查 Streamlit
```bash
python -c "import streamlit; print(streamlit.__version__)"
```

### 检查所有依赖
```bash
python -c "import streamlit, langchain, langchain_openai, openai, dotenv; print('所有依赖已安装')"
```

## 🚀 启动应用

安装完成后，运行：
```bash
run.bat
```

或手动启动：
```bash
python -m streamlit run app.py
```

## 🔧 常见问题

### 问题 1: 'python' 不是内部或外部命令

**解决方案：**
- Python 未添加到 PATH
- 重新安装 Python 并勾选 "Add Python to PATH"
- 或手动添加 Python 到系统环境变量

### 问题 2: pip 不是内部或外部命令

**解决方案：**
```bash
python -m pip install --upgrade pip
```

### 问题 3: 安装依赖时权限错误

**解决方案：**
- 使用管理员权限运行命令提示符
- 或使用用户安装：
```bash
python -m pip install --user -r requirements.txt
```

### 问题 4: 网络连接问题

**解决方案：**
- 使用国内镜像源（推荐）：
```bash
python -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 问题 5: 版本冲突

**解决方案：**
- 创建虚拟环境（推荐）：
```bash
python -m venv venv
venv\Scripts\activate
python -m pip install -r requirements.txt
```

## 📝 项目依赖说明

- **streamlit**: Web 应用框架
- **langchain**: LLM 应用开发框架
- **langchain-openai**: LangChain 的 OpenAI 集成
- **openai**: OpenAI API 客户端
- **python-dotenv**: 环境变量管理

## 💡 推荐：使用虚拟环境

为了避免依赖冲突，建议使用虚拟环境：

```bash
# 创建虚拟环境
python -m venv venv

# 激活虚拟环境（Windows）
venv\Scripts\activate

# 安装依赖
python -m pip install -r requirements.txt

# 运行应用
python -m streamlit run app.py
```

## 📞 需要帮助？

如果遇到问题，请检查：
1. Python 版本是否为 3.8 或更高
2. pip 是否正常工作
3. 网络连接是否正常
4. 是否有足够的磁盘空间

